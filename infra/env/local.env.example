# Valores exclusivamente locales y sintéticos. No contienen secretos reutilizables.
POSTGRES_USER=housekeeper
POSTGRES_PASSWORD=local-development-only
POSTGRES_DB=housekeeper
APP_DB_PASSWORD=local-app-only
WORKER_DB_PASSWORD=local-worker-only
AUTH_DB_PASSWORD=local-auth-only
BETTER_AUTH_SECRET=local-better-auth-secret-change-before-use
# Se entra con usuario y contraseña. Las cuentas se dan de alta con
#   pnpm --filter @housekeeper/web seed:accounts --config <json fuera del repo>
# que necesita además SEED_DATABASE_URL con el propietario de las migraciones.
# Ninguna contraseña vive en este fichero ni en el repositorio.
MINIO_ROOT_USER=housekeeper-local
MINIO_ROOT_PASSWORD=local-object-storage-only
S3_BUCKET=housekeeper-local
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=local-observability-only
CADDY_HTTP_PORT=8080
WEB_HEALTH_PATH=/api/health
